# Cloudflare Pages response headers.
# https://developers.cloudflare.com/pages/configuration/headers/

# Astro emits content-hashed assets that are safe to cache forever.
/_astro/*
  Cache-Control: public, max-age=31536000, immutable

# Thumbnails are content-hashed by filename (v2, v3, -fa). Cache aggressively;
# bump the filename when replacing an image to bust stale CDN entries.
/videos/thumbs/*
  Cache-Control: public, max-age=31536000, immutable

/fonts/*
  Cache-Control: public, max-age=31536000, immutable

# Sensible security defaults for every HTML response.
/*
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  X-Frame-Options: SAMEORIGIN
